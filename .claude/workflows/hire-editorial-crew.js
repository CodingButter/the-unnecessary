export const meta = {
  name: 'hire-editorial-crew',
  description: 'Hire the four editorial roles the crew-roles audit flagged as GAPS: a developmental/structural editor (HIGH), a copy editor + style-sheet owner (MED-HIGH), a sensitivity/authenticity reader (MED, triage-only with an explicit human caveat), and a terminal cold-read proofreader (MED). Creates each agent charter in the house format (crew-handbook pointer + field-notes file), per the audit spec, as read-only DIAGNOSERS that route findings to the adjudicator (never editing prose themselves). Logs Decision 063 and validates. Does NOT wire them into the gauntlet -- that is a deliberate follow-up.',
  phases: [
    { title: 'Hire', detail: 'create the 4 editorial agent charters + their field-notes files (parallel)' },
    { title: 'Govern', detail: 'Decision-log entry recording the hires + the wiring follow-up; validate' },
  ],
}

const NOVEL = '/home/codingbutter/Novel'
async function tryAgent(make, tries) { tries = tries || 3; let last; for (let i = 0; i < tries; i++) { try { const r = await make(); if (r) return r; last = new Error('empty result'); } catch (e) { last = e; log('retry ' + (i + 1) + '/' + tries + ': ' + String(e).slice(0, 140)); } } throw last; }
const REPORT = { type: 'object', properties: { ok: { type: 'boolean' }, summary: { type: 'string' }, details: { type: 'string' } }, required: ['ok', 'summary'] }

const HOUSE = `HOUSE FORMAT + CONVENTIONS (match exactly):\n` +
  `- First read 2 existing charters for the format + house voice: ${NOVEL}/.claude/agents/clarity-auditor.md (read-only diagnoser that routes to the adjudicator -- the closest analog) and ${NOVEL}/.claude/agents/sound-engineer.md (a recent hire). Also read ${NOVEL}/.claude/crew-handbook.md so you know the shared directives you must NOT repeat.\n` +
  `- Frontmatter: name (kebab matching the file), a sharp one-line description starting "Reach for this when ..." that distinguishes this role from the agents nearest it, tools (exactly as specified below), model: inherit.\n` +
  `- Near the TOP of the body, the firm crew-handbook pointer (copy the form the other charters use): "You MUST read the crew handbook at .claude/crew-handbook.md before working -- it carries the shared crew directives (autonomous resolution, field notes, canon safety, project context) and they apply to you." Do NOT re-state the handbook's content; keep the charter to this role's unique job.\n` +
  `- This crew runs a DIAGNOSE-then-APPLY split (the audit praised it): reviewers FLAG, the single adjudicator APPLIES. So this agent DIAGNOSES and returns findings/a letter routed to the adjudicator; it does NOT edit manuscript prose itself (the one exception noted per-role below).\n` +
  `- Then create this agent's empty field-notes file at ${NOVEL}/.claude/agent-notes/<name>.md with the header "# Field Notes -- <name>" and the standard one-line blockquote contract (copy it from an existing notes file under .claude/agent-notes/), body empty.\n` +
  `- Avoid em dashes in any drafted prose. Report (REPORT schema): ok, the charter path + its one-line description, and 2 lines on how its lane differs from the nearest existing agent.`

const HIRES = [
  { name: 'developmental-editor', tools: 'Read, Grep, Glob',
    spec: `ROLE: DEVELOPMENTAL / STRUCTURAL EDITOR (the crew's biggest gap; audit priority HIGH). The whole existing gauntlet works at the scene/sentence layer; NOBODY reads the ASSEMBLED book for ARCHITECTURE. This agent is read-only and reads the WHOLE assembled draft (an act/section, or the blueprint SET before a section is drafted) for: overall arc, pacing (sagging middles, rushed turns), structure and chapter ORDER, character-arc motivation across chapters, theme coherence and drift, setup/payoff balance, and "what to cut, expand, or reorder." It delivers a DEVELOPMENTAL REVISION LETTER (big-picture, prioritized), NOT line edits and NOT per-scene craft notes. Findings route to the adjudicator; anything needing FRESH scene prose routes by name to chapter-drafter. It runs at TWO moments: on the blueprint set before a section is drafted, and on the assembled chapters of a completed act/section -- NOT per-chapter. LANE: distinct from blueprint-author (which PLANS architecture pre-prose, it does not critique the REALIZED architecture), from focus-reviewer (per-entity emphasis, not whole-arc shape), and from logic-auditor (local plot-logic / effect-without-cause, explicitly NOT arc/pacing/structure/theme). Tools: Read, Grep, Glob (read-only).` },
  { name: 'copy-editor', tools: 'Read, Grep, Glob, Write, Edit',
    spec: `ROLE: COPY EDITOR + STYLE-SHEET OWNER (audit priority MED-HIGH). Owns the mechanical error class that erodes credibility on every page: grammar, spelling, punctuation, usage, homophones (being/begin, sipped/shipped), inconsistent hyphenation/capitalization, missing/doubled words, number and treatment consistency. DIAGNOSES these on the prose and routes findings to the adjudicator -- it does NOT edit manuscript prose itself (preserve the diagnose/apply split). The ONE thing it WRITES (hence Write/Edit): it MAINTAINS and EXTENDS the project's single house STYLE SHEET (the existing canonical doc docs/10-vision/style/formatting.md -- do NOT create a second style-sheet doc; add a Copy-Edit Consistency Ledger to it and preserve its frontmatter so validators pass) pinning house spellings, hyphenation, number rules, capitalization, and recurring term treatments; this style sheet also feeds the audio agents. LANE: distinct from prose-critic (CRAFT/style -- voice, rhythm, cliche; the industry deliberately splits style from mechanics) and from continuity-auditor (FACT consistency, not punctuation/usage). Enforce the project no-em-dash rule as part of mechanics. Tools: Read, Grep, Glob, Write, Edit (Write/Edit for the STYLE SHEET only -- never the manuscript prose).` },
  { name: 'sensitivity-reader', tools: 'Read, Grep, Glob',
    spec: `ROLE: SENSITIVITY / AUTHENTICITY READER (audit priority MED; advisory TRIAGE only). The book is grounded cyberpunk in Greater Detroit centered on class, race, disability, and abandoned/unprofitable communities -- exactly this role's material, and the crew has no lived-standpoint lens. On a chapter that depicts a specific identity or lived experience, it flags two lenses the audit drew from the EFA: AUTHENTICITY (portrayals that might MISREPRESENT -- accuracy) and SENSITIVITY (portrayals that might OFFEND or do harm -- stereotypes, harmful tropes, unintentional bias). CRITICAL CAVEAT to state plainly IN THE CHARTER: a synthetic agent can only APPROXIMATE a sensitivity read; for anything load-bearing this points at a HUMAN reader, not an agent -- the agent's value is TRIAGE (surface candidates early), never sign-off. It is ADVISORY ONLY: the author decides; findings are routed as flags, NEVER as silent edits, and it never blocks. LANE: no other agent holds the lived standpoint; the craft/continuity agents lack it by definition. Tools: Read, Grep, Glob (read-only, advisory).` },
  { name: 'cold-reader', tools: 'Read',
    spec: `ROLE: COLD-READ PROOFREADER (audit priority MED; the LAST net). A terminal, fresh-eyes, errors-ONLY pass on the FINALIZED prose AND on the narration/cue scripts, run AFTER the adjudicator finalizes a chapter. Catches the granular slips everyone upstream has gone blind to through familiarity (homophones, dropped or transposed words, doubled words, a misspelled character/place name, a broken/contradicted micro-detail) PLUS errors INTRODUCED by later stages (an edit that left a dangling clause, a narration tag that dropped a word). Its ENTIRE value is being the one agent that did NOT see the chapter before, so it must be given NO earlier-stage context (no diagnosis history, no revision log) -- and it CANNOT be the adjudicator, which authored the edits (familiarity blindness is the whole point). It reports an errors-only list routed to the adjudicator for application; no craft, no story, no continuity judgement. LANE: distinct from clarity-auditor (comprehension), prose-critic (craft), continuity-auditor (canon facts) -- this is pure mechanical fresh-eyes proofing. Tools: Read ONLY (deliberately minimal; denied broader context by design).` },
]

phase('Hire')
log('hire-editorial-crew: creating ' + HIRES.length + ' editorial charters in parallel')
const hires = await parallel(HIRES.map(h => () => agent(
  `Create a new specialist agent charter for the novel-production crew of "The Unnecessary" at ${NOVEL}/.claude/agents/${h.name}.md, plus its field-notes file. This is an editorial role the crew-roles audit (docs/70-research/crew-roles-audit.md) flagged as a GAP.\n\n` +
  h.spec + `\n\nTOOLS for this agent: ${h.tools}.\n\n` + HOUSE,
  { agentType: 'general-purpose', label: 'hire:' + h.name, phase: 'Hire', schema: REPORT }
)))
const made = HIRES.filter((h, i) => hires[i]).map(h => h.name)
log('hired: ' + made.join(', ') + (made.length < HIRES.length ? ' (' + (HIRES.length - made.length) + ' failed)' : ''))

phase('Govern')
const gov = await tryAgent(() => agent(
  `Record the editorial-crew expansion for "The Unnecessary" in the decision log and validate. The crew just hired ${made.length} editorial agents (${made.join(', ')}) from the crew-roles audit's recommended GAPS.\n` +
  `1. Add a Decision-log entry under ${NOVEL}/docs/00-governance/decision-log/decisions/ (match the existing format + the next number after 062, likely 063) titled for the editorial-crew hires. Record: WHAT (the ${made.length} roles + one line each on what each catches), WHY (grounded in docs/70-research/crew-roles-audit.md -- arc/structure, mechanics+style-sheet, sensitivity triage, terminal cold-read; each a distinct error class no existing agent owned), the SENSITIVITY-READER CAVEAT (synthetic approximates; load-bearing -> human; triage not sign-off), and an explicit FOLLOW-UP note that these agents are HIRED but NOT yet wired into the gauntlet/workflows -- wiring is the next deliberate step (copy-editor + cold-reader fit the per-chapter gauntlet; developmental-editor runs at SECTION/book cadence, not per-chapter; sensitivity-reader is conditional on identity-depicting chapters). Update the decision-log index (increment the count + add the row).\n` +
  `2. Run \`python3 scripts/validate-metadata.py\` and \`python3 scripts/validate-links.py\` from ${NOVEL}; report PASS/FAIL + any error lines verbatim. The new Decision doc must be well-formed.\n` +
  `Report (REPORT schema): ok (true only if both validators pass with 0 errors), the Decision path, and the validator results.`,
  { schema: REPORT, phase: 'Govern', agentType: 'general-purpose' }
))

return { hired: made, hires, govern: gov }
