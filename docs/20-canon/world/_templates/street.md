---
title: "[Display Name]"
document_type: "entity"
entity_type: "street"
status: "stub"
authority: "world-canon"
parent: [city-id]
summary: "[one line: the named run this street is]"
tags:
  - world
  - street
  - network
related:
  - "../../../00-governance/entity-spec.md"
source_documents:
  - "docs/00-governance/entity-spec.md"
---

# [Display Name]

> Copy this file to `<id>.md` in the city's network area, rename the heading, and
> replace the placeholders. A street is a NETWORK entity: its `parent` is only a
> browsing home — its real extent is COMPUTED by walking the segments whose `street:`
> points back here (spec §5, §7). Never list segments or addresses; those are derived.
> CEILING, not floor: a stub is the frontmatter plus the one-line Overview. Raise
> `status` to `active-canon` when approved. Tag gated detail with reveal markers.

## Overview

[One sentence naming the street and its run end to end.]

## Character of the Street

[What this street *is* as a place — its reputation, rhythm, who walks it. The street is
describable; this is its character, not an inventory of what sits on it.]

## Condition and Surface

[Paving, lighting, repair state, how the withdrawal of its district shows here.]

## What Lives Along It

[The felt frontage — the kinds of buildings and life that face it. The exact address
list is generated by walking `addressed-to` edges, so describe character, not a roster.]

## History

[How the street got its name and present shape. Datable changes go in the timeline.]

```yaml
# A street owns little hard data of its own; its run and frontage are DERIVED (spec §5, §7).
edges:
  # neighbor: [parallel-or-adjoining-street-id]   # symmetric, optional
facts: {}
timeline: []   # streets rarely carry state — add a dated entry only if the run itself changes (renamed, closed)
```
